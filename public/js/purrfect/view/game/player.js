/*global _li*/

(function (module) {
    'use strict';

    var moduleName = module.get('name'),
        add,
        init;

    init = function () {

    };

    add = function() {

    };

    module.subscribe(moduleName, 'main', init);

}(_li.define('purrfect.view.game.player')));